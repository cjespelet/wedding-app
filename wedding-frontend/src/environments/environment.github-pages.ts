export const environment = {
  production: true,
  // Backend en subdominio aparte (GitHub Pages solo sirve estático).
  // Sobrescribible en CI con API_BASE_URL.
  apiBaseUrl: 'https://api.jesiyjavier.com.ar/api',
};
